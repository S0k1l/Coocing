@model IEnumerable<Recipes>

@{
    bool isAuthenticated = User.Identity?.IsAuthenticated == true;
}

<section class="space-sm flush-with-above">
    <div class="container">
        <ul class="row feature-list feature-list-sm">
            @* @foreach (var club in Model) *@
            @for (int i = 0; i < 10; i++)
            {
                <li class="col-12 col-md-6 col-lg-4" style="list-style-type: none;">
                    <div class="card">
                        <a type="button" asp-controller="Recipes" asp-action="Details" asp-route-id="1">
                            <img class="card-img-top" src="https://images.immediate.co.uk/production/volatile/sites/30/2021/03/borscht-recipe-ab000dc.jpg?quality=90&resize=556,505" alt="Card image cap">
                        </a>
                        <div class="card-body">
                            <h4 class="card-title">Борщ</h4>
                            <p class="card-text">Смачно шо капець</p>
                        </div>
@*                         <div class="card-footer card-footer-borderless d-flex justify-content-between">
                            @if (isAuthenticated && User.IsInRole("admin"))
                            {
                                <div>
                                    <a type="button" class="btn btn-sm btn-outline-info" asp-controller="Club" asp-action="Edit" asp-route-id="@club.Id">
                                        Edit
                                    </a>
                                    <a type="button" class="btn btn-sm btn-outline-danger" asp-controller="Club" asp-action="Delete" asp-route-id="@club.Id">
                                        Delete
                                    </a>
                                </div>
                            }
                            <div>
                                <small>@club.</small>
                            </div>
                        </div> *@
                    </div>
                    <br/>
                </li>
            }
        </ul>
    </div>
</section>